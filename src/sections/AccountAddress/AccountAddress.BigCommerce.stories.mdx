import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs'

import {
  getCustomerDecorator,
  getPlatformDecorator,
  getQueryDecorators,
} from '../../lib/decorators'
import AccountAddress from '.'
import Description from '.'

<Meta
  title="Sections/BigCommerce/AccountAddress"
  component={AccountAddress}
  decorators={[getPlatformDecorator('big_commerce')]}
/>

# Header

<Description />

## Props

<ArgsTable of={AccountAddress} />

## Examples

### Create

<Preview>
  <Story name="Create" decorators={[getCustomerDecorator('bill@bigcommerce-platform.com')]}>
    {args => <AccountAddress {...args} />}
  </Story>
</Preview>

### Edit

<Preview>
  <Story
    name="Edit"
    parameters={{ query: { id: '359276' } }}
    decorators={[
      getCustomerDecorator('bill@bigcommerce-platform.com'),
      ...getQueryDecorators(
        {
          query: { id: '359276' },
        },
        '/account/address',
      ),
    ]}
  >
    {args => <AccountAddress {...args} />}
  </Story>
</Preview>
