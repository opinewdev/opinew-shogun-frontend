import { Meta, Preview, Story } from '@storybook/addon-docs'

import {
  getPlatformDecorator,
  getCustomerDecorator,
  getQueryDecorators,
} from '../../lib/decorators'
import { ACCOUNT_CHANGE_PASSWORD_URL } from '../../components/Data'
import { uniqueId } from '../../components/Utils'

import ChangePasswordForm from '.'

<Meta
  title="Sections/BigCommerce/ChangePasswordForm"
  component={ChangePasswordForm}
  decorators={[getPlatformDecorator('big_commerce')]}
/>

# ChangePasswordForm

A section that allows users to define a new password.

## Examples

### Default

<Preview>
  <Story
    name="Default"
    decorators={[
      getCustomerDecorator('bill@bigcommerce-platform.com'),
      ...getQueryDecorators({ query: { dummy: uniqueId() } }, ACCOUNT_CHANGE_PASSWORD_URL),
    ]}
  >
    <ChangePasswordForm />
  </Story>
</Preview>
