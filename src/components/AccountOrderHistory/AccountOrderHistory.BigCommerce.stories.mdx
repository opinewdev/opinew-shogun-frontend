import { Meta, Preview, Story } from '@storybook/addon-docs'
import AccountOrderHistory from '.'
import getDecorators from './decorators'
import Description from './description.mdx'
import { getPlatformDecorator } from '../../lib/decorators'

<Meta
  title="UI Primitives/BigCommerce/AccountOrderHistory"
  parameters={{ layout: 'padded' }}
  decorators={[getPlatformDecorator('big_commerce')]}
/>

# AccountOrderHistory

<Description />

## Examples

### Default

<Preview>
  <Story
    name="Default"
    decorators={getDecorators('bill@bigcommerce-platform.com')}
    parameters={{
      docs: {
        source: {
          code: '<AccountOrderHistory orders={orders} />',
        },
      },
    }}
  >
    <AccountOrderHistory />
  </Story>
</Preview>
